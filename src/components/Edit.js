import React from 'react'

const Edit = () => {
  return (
    <div>
        <div>
            {userEvent.map((i)=>
           //popup for info editing
           <h1></h1>
            )}
        </div>
    </div>
  )
}

export default Edit